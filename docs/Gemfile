source 'https://rubygems.org'

# Mirror the exact gem set GitHub Pages runs in production so local + CI builds
# behave like the published site. See:
# https://pages.github.com/versions/
gem 'github-pages', group: :jekyll_plugins
