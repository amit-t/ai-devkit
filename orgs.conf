# ai-devkit org list
# One GitHub org slug per line. `#` starts a comment.
# The org that hosts this devkit checkout (inferred from its `origin` remote)
# is always included automatically — you do NOT need to list it here.
#
# Edit directly, or use: orgs.wb add <slug> / orgs.wb remove <slug>

Invenco-Cloud-Systems-ICS
